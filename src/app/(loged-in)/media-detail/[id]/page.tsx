export default function MediaDetailPage({ params }: { params: string }) {
  const { id } = params;
  return (
    <>
      <h1>MediaDetailPage Component</h1>
      {/* TODO
      1. id 가지고 영화 상세 정보 fetch
      2. /my-list에 추가하는 기능
      */}
    </>
  );
}
